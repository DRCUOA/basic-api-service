run @map_structure.sh to update the structure diagram, use next version name from the sequence in@api-server/documentation/40-structure and save in the same dir.  Then update @api-server/structure-changelog.md to reflect only the structure changes. Then iterate package-json version number.  Then capture and analyse all diffs from main remote and log them in @api-server/CHANGELOG.md 